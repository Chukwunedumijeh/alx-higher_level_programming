#!/usr/bin/python3
def weight_average(my_list=[]):
    if not my_list:
        return 0
    res = 0.0
    s_list = 0
    w_list = 0

    S_list += (t[0] + t[1] for t in my_list)
    w_list += (t[1] for t in my_list)
    res = (s_list) / (w_list)
    return res
